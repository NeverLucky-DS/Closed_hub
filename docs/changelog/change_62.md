# Change 62

## Что изменилось

- Превью папок в библиотеке переключено с `папка.jpeg` на новый файл `файл.webp`.
- Route для картинки библиотеки переименован в `/library-folder-icon.webp`.
- Иконка папки в карточке стала немного больше.

## Зачем

Новая картинка лучше подходит под текущий визуальный стиль библиотеки, а `.webp` route совпадает с фактическим форматом файла.
