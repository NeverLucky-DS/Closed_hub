# change_41

## Что подправлено (мобильная библиотека)

- Панель «назад»: без длинного текста под ссылкой, спокойная граница без сильного blur; подсказка в `title` у ссылки.
- Меньше визуального шума: разделители `border-ink-600/50`, убран дубль кнопки «Сохранить» внизу у PDF (осталась одна сверху).
- Подсказки про iOS свёрнуты в **`<details>`** «Совет для iPhone» — по умолчанию экран ниже, контраст текста выше (`text-ink-300`).
- Имя файла на телефоне компактнее (`text-xs`, `line-clamp-2`), тип MIME в метаданных скрыт на мобилке.
- Блок PDF: только строка статуса + страницы, без абзаца перед ними.

Десктоп не трогали.
