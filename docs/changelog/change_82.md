# change_82 — сведение веток к `main`

**Что сделано:** локальный `main` fast-forward до `agent1` (`4e9c5b9`), затем `git push origin main`. На `main` уже были слиты интеграционные коммиты (hardening, resources, private profile, bot search и т.д.), отдельные старые ветки с одним MVP-коммитом не добавлялись повторно — достаточно одной линии `main`.

**Зачем:** одна актуальная линия разработки на GitHub и локально, без расхождения `main` и рабочей ветки агента.
