# Change 59

## Что изменилось

- Основной шрифт сайта заменён на `VCROSDMonoRUSbyD`.
- Старый `Guildenstern NBP` больше не используется для бренда.
- Рамка и ring-окантовка вокруг `ML Hub` в верхней шапке и sidebar убраны.

## Зачем

Весь интерфейс должен использовать один выбранный шрифт, а название `ML Hub` должно выглядеть как чистая надпись без лишней рамки.
