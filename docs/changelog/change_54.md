# Change 54

## Что изменилось

- В светлой теме hover-состояния `hover:text-white` и `group-hover:text-white` теперь остаются тёмными.

## Зачем

На белых карточках текст не должен исчезать при наведении. Тёмные темы при этом сохраняют прежнее поведение.
