# change_16

## Что сделано

- В **`utils/company_sheet.py`**: функция **`hr_workbook_sheet_title`** — если компания пустая или похожа на «не указано» / `unknown` / `—` и т.п., вкладка книги = **`НЕИЗВЕСТНО`**; иначе как раньше **`normalize_company_sheet_title`**.
- **`append_hr_contact_row`** переведён на **`hr_workbook_sheet_title`**.

## Зачем

- Все HR без работодателя попадают на один лист с понятным именем, без размазывания по «без компании» в разном регистре.

## Позже

- При необходимости вынести список «пустых» формулировок в JSON.
